@model List<WebPhongKham.Models.ViewModels.PhieuKhamViewModel>
@{
    Layout = "_Layout";
}

<div class="container mt-5" style="max-width: 700px;">
    <h3 class="text-center mb-4">Tra cứu lịch hẹn</h3>

    <form method="get" class="mb-4">
        <div class="input-group">
            <input class="form-control"
                   name="sdt"
                   placeholder="Nhập số điện thoại đã đặt lịch..."
                   value="@ViewBag.SDT"
                   required />
            <button class="btn btn-primary" type="submit">Tra cứu</button>
        </div>
    </form>

    @if (Model != null)
    {
        if (Model.Count == 0 && !string.IsNullOrEmpty(ViewBag.SDT))
        {
            <div class="alert alert-warning text-center">
                Không tìm thấy lịch hẹn nào với số điện thoại này.
            </div>
        }
        else if (Model.Count > 0)
        {
            <h5 class="mb-3">Kết quả tra cứu:</h5>

            <div class="list-group">
                @foreach (var lh in Model)
                {
                    <div class="list-group-item shadow-sm mb-2 rounded">
                        <div class="d-flex justify-content-between align-items-center">
                            <div>
                                <div class="fw-bold text-primary">
                                    📅 @lh.NgayKham.ToString("dd/MM/yyyy") - 🕒 @lh.GioKham.ToString(@"hh\:mm")
                                </div>

                                <div class="mt-1">
                                    👨‍⚕️ Bác sĩ: <b>@lh.TenBacSi</b>
                                </div>

                                <div class="mt-1">
                                    Trạng thái:
                                    @if (lh.TrangThai == "Đã khám")
                                    {
                                        <span class="badge  text-white fw-semibold px-3 py-2 bg-success">@lh.TrangThai</span>
                                    }
                                    else if (lh.TrangThai == "Đã đặt")
                                    {
                                        <span class="badge  text-white fw-semibold px-3 py-2 bg-warning text-dark">@lh.TrangThai</span>
                                    }
                                    else if (lh.TrangThai == "Đã hủy")
                                    {
                                        <span class="badge text-white fw-semibold px-3 py-2 bg-danger">@lh.TrangThai</span>
                                    }
                                    else
                                    {
                                        <span class="badge text-white fw-semibold px-3 py-2 bg-secondary">@lh.TrangThai</span>
                                    }
                                </div>
                            </div>

                            <div class="text-end">
                                <a class="btn btn-outline-primary btn-sm"
                                   href="/DatLich/PhieuKhamByMa?ma=@lh.PhieuKham">
                                    Xem phiếu
                                </a>
                            </div>
                        </div>
                    </div>
                }
            </div>
        }
    }
</div>
